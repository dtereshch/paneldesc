% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summarize_numeric.R
\name{summarize_numeric}
\alias{summarize_numeric}
\title{Summary Statistics for Numeric Variables}
\usage{
summarize_numeric(
  data,
  select = NULL,
  group = NULL,
  detail = FALSE,
  digits = 3
)
}
\arguments{
\item{data}{A data.frame containing variables for analysis.}

\item{select}{A character vector specifying which numeric variables to analyze.
If not specified, all numeric variables in the data.frame will be used.}

\item{group}{A character string specifying the grouping variable name.
If not specified, overall statistics will be returned.}

\item{detail}{A logical flag indicating whether to return additional statistics (25th, 50th, and 75th percentiles).
Default = FALSE.}

\item{digits}{An integer specifying the number of decimal places for rounding statistics.
Default = 3.}
}
\value{
A data.frame with descriptive statistics summary.
}
\description{
This function calculates summary statistics for numeric variables,
either overall or grouped by a single grouping variable.
}
\details{
The returned data.frame contains columns depending on the arguments:

When no grouping variable is specified (overall):
\describe{
\item{\code{variable}}{The name of the numeric variable.}
\item{\code{count}}{Number of non‑NA observations.}
\item{\code{mean}}{Arithmetic mean.}
\item{\code{std}}{Standard deviation.}
\item{\code{min}}{Minimum value.}
\item{\code{max}}{Maximum value.}
}

When \code{detail = TRUE}, additional columns are included:
\describe{
\item{\code{p25}}{25th percentile (first quartile).}
\item{\code{p50}}{50th percentile (median).}
\item{\code{p75}}{75th percentile (third quartile).}
}

When a grouping variable is specified, statistics are calculated for each group,
and the data.frame includes a column named after the grouping variable,
followed by the same statistics columns as above.

The object has class \code{"panel_summary"} and two additional attributes:
\describe{
\item{\code{metadata}}{List containing the function name and the arguments used.}
\item{\code{details}}{List with counts of variables, groups, and total observations.}
}
}
\note{
If the input data is a \code{panel_data} object (e.g., from the \strong{panelr} package),
the function automatically retrieves the entity and time identifiers from its
metadata. These variables are then excluded from the default selection of
numeric variables (i.e., when \code{select = NULL}) because they typically do
not represent substantive measurements. However, if you explicitly include
them in the \code{select} argument, they will be analyzed. The \code{group} argument
for grouping summaries is independent and can be set to any variable,
including the panel's time or entity variable.

This function does \strong{not} use panel attributes for grouping; it is designed for general use.
}
\examples{
data(production)

# Basic usage
summarize_numeric(production)

# Selecting specific variables
summarize_numeric(production, select = "sales")
summarize_numeric(production, select = c("capital", "labor"))

# Grouped statistics
summarize_numeric(production, group = "year")

# Detailed statistics
summarize_numeric(production, detail = TRUE)

# Custom rounding
summarize_numeric(production, digits = 2)

# Accessing attributes
out_sum_num <- summarize_numeric(production)
attr(out_sum_num, "metadata")
attr(out_sum_num, "details")

}
\seealso{
\code{\link[=decompose_numeric]{decompose_numeric()}} for variance decomposition.
\code{\link[=decompose_factor]{decompose_factor()}} for factor variable decomposition.
\code{\link[=summarize_transition]{summarize_transition()}} for transition summaries.
\code{\link[=summarize_missing]{summarize_missing()}} for missing value summaries.
}
