% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/make_panel.R
\name{make_panel}
\alias{make_panel}
\title{Panel Data Structure Setting and Balancing}
\usage{
make_panel(data, group, time, interval = NULL, balance = NULL)
}
\arguments{
\item{data}{A data.frame containing panel data in a long format.}

\item{group}{A character string specifying the name of the entity/group variable.}

\item{time}{A character string specifying the name of the time variable.}

\item{interval}{An optional positive integer giving the expected interval between time periods.}

\item{balance}{One of \code{NULL} (default), \code{"entities"}, \code{"periods"}, or \code{"all"}. If not \code{NULL},
the panel is balanced according to the chosen method (see Details).}
}
\value{
The input data.frame with additional attributes, after possibly filtering or expanding rows.
}
\description{
This function adds panel structure attributes to a data.frame, storing entity/group and time variable names,
and optionally checks the expected interval between time periods. It can also balance the panel by
keeping only entities present in all periods, keeping only periods where all entities are present,
or creating all entity‑time combinations (filling missing combinations with \code{NA}).
}
\details{
This function adds attributes to a data.frame to mark it as panel data.
The returned object has class \code{"panel_data"} (in addition to its original class).
It includes the following attributes:
\describe{
\item{\code{metadata}}{List containing the function name and the arguments used
(\code{group}, \code{time}, \code{interval}, and \code{balance} if provided).}
\item{\code{details}}{List with diagnostic vectors. By default it contains:
\describe{
\item{\code{entities}}{Unique values of the group variable.}
\item{\code{periods}}{Sorted unique values of the time variable.}
}
If any rows were removed due to missing values in \code{group} or \code{time}, an additional component is included:
\describe{
\item{\code{excluded_rows}}{A data frame containing the rows that were removed because they
contained \code{NA} in either the group or time variable.}
}
If duplicates of the group-time combination are found, an additional vector is included:
\describe{
\item{\code{entity_time_duplicates}}{A data frame containing the distinct duplicate
combinations of group and time.}
}
If \code{interval} is supplied and missing periods are detected, two additional vectors are included:
\describe{
\item{\code{periods_restored}}{The full sequence of periods from \code{min(time)} to \code{max(time)} by \code{interval}.}
\item{\code{periods_missing}}{The periods in \code{periods_restored} that are not present in the data.}
}}
}

The function first checks for missing values in the \code{group} and \code{time} variables.
Rows containing \code{NA} in either variable are removed, and messages report how many rows were excluded
due to each variable. The removed rows are stored in \code{details$excluded_rows}.

It then checks for duplicate group-time combinations. In a properly structured panel dataset,
each entity (group) should have at most one observation per time period. If duplicates are found,
a message is printed with the number of distinct duplicate combinations and up to five examples.
The duplicate combinations are stored in \code{details$entity_time_duplicates}. \strong{Note:} When a
\code{balance} method other than \code{NULL} is requested, duplicates are automatically removed (the first
occurrence is kept) and a message informs the user.

When \code{interval} is specified, the function first attempts to convert the time variable to numeric
(if it is not already). If conversion fails, an error is raised. It then checks whether the observed
time points are compatible with a regular spacing of that interval. If any observed difference is not
a multiple of the interval, an error is raised. If all differences are multiples of the interval but
there are gaps, a message is printed listing the missing periods, and the analysis continues.
In this case, the restored full sequence and the missing periods are stored in
\code{details$periods_restored} and \code{details$periods_missing}, respectively.

\strong{Balancing the panel}

If \code{balance} is not \code{NULL}, the panel is balanced according to the chosen method.
The definition of \strong{presence} follows that of \code{\link[=describe_patterns]{describe_patterns()}}: an entity is considered present
in a time period if there exists at least one row with that combination and at least one non-\code{NA}
value in any substantive variable (i.e., all columns except \code{group} and \code{time}).

\describe{
\item{\code{balance = "entities"}}{Keep only those entities that are present in \strong{all} time periods.
Rows belonging to entities that miss at least one period are removed.}
\item{\code{balance = "periods"}}{Keep only those time periods for which \strong{all} entities are present.
Rows with time values not satisfying this condition are removed.}
\item{\code{balance = "all"}}{Create a row for every combination of entity and time period. If a combination
already exists, the corresponding row(s) are kept (if duplicates exist, only the first is kept);
if it does not exist, a new row is added with \code{NA} in all other columns.
If \code{interval} is supplied, the full time sequence from \code{min(time)} to \code{max(time)} by \code{interval}
is used, so that missing periods are also filled.}
}

After balancing, the \code{details$entities} and \code{details$periods} components are updated to reflect
the new data. No additional vectors are added for the removed entities/periods, but the information
is implicitly contained in the filtered data.
}
\examples{
data(production)

# Add panel attributes without interval check
panel_data <- make_panel(production, group = "firm", time = "year")

# Check the attributes
attr(panel_data, "metadata")
attr(panel_data, "details")

# Use with describe_dimensions()
describe_dimensions(panel_data)

# With interval specification (assuming yearly data)
panel_data2 <- make_panel(production, group = "firm", time = "year", interval = 1)

# Keep only firms present in every year
balanced_entities <- make_panel(production, group = "firm", time = "year",
                                balance = "entities")

# Keep only years where all firms are observed
balanced_periods <- make_panel(production, group = "firm", time = "year",
                               balance = "periods")

# Create a fully balanced panel (all firm‑year combinations)
fully_balanced <- make_panel(production, group = "firm", time = "year",
                             balance = "all", interval = 1)

}
\seealso{
\code{\link[=describe_dimensions]{describe_dimensions()}}, \code{\link[=describe_balance]{describe_balance()}}, \code{\link[=describe_periods]{describe_periods()}}, \code{\link[=describe_patterns]{describe_patterns()}}
}
