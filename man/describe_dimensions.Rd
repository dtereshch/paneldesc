% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/describe_dimensions.R
\name{describe_dimensions}
\alias{describe_dimensions}
\title{Panel Data Dimensions Description}
\usage{
describe_dimensions(data, index = NULL)
}
\arguments{
\item{data}{A data.frame containing panel data in a long format.}

\item{index}{A character vector of length 2 specifying the names of the entity and time variables.
Not required if data has panel attributes.}
}
\value{
A data.frame containing panel dimension counts.
}
\description{
This function provides basic dimension counts for panel data:
number of rows, unique entities, unique time periods, and substantive variables.
}
\details{
The returned data.frame has the following structure:
\itemize{
\item{\code{rows}}{ Total number of rows in the data frame.}
\item{\code{entities}}{ Number of distinct values in the entity variable.}
\item{\code{periods}}{ Number of distinct values in the time variable.}
\item{\code{variables}}{ Number of substantive variables (all columns except entity and time).}
}

The object has class \code{"panel_description"} and two additional attributes:
\describe{
\item{\code{metadata}}{List containing the function name and the arguments used.}
\item{\code{details}}{List with the actual vectors of entities, periods, and substantive variables.}
}
}
\examples{
data(production)

# Basic usage
describe_dimensions(production, index = c("firm", "year"))

# With panel_data object
panel <- make_panel(production, index = c("firm", "year"))
describe_dimensions(panel)

# Accessing attributes
out_des_dim <- describe_dimensions(production, index = c("firm", "year"))
attr(out_des_dim, "metadata")
attr(out_des_dim, "details")

}
\seealso{
See also \code{\link[=describe_balance]{describe_balance()}}, \code{\link[=describe_periods]{describe_periods()}}.
}
