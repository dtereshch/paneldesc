% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_means.R
\name{add_means}
\alias{add_means}
\title{Add Group Means to a Data Frame}
\usage{
add_means(data, group = NULL)
}
\arguments{
\item{data}{A data.frame containing the variables.}

\item{group}{A character string or vector of character strings specifying
the grouping variable(s).
If not specified and data is a \code{panel_data} object, the entity and time values
will be extracted from the data.frame attributes to define grouping variables.
Otherwise, overall means are computed and added as constant columns.}
}
\value{
The input data frame with additional columns for group means.
}
\description{
This function adds group means of numeric variables to a data frame, which can
be used for Mundlak-style (correlated random effects) modeling or for other purposes where
within-group averages are needed.
}
\details{
For each numeric variable (excluding the grouping variables) and for each
grouping variable, the group mean is computed (ignoring \code{NA}s) and added
as a new column.

For a numeric variable \code{x} and a grouping variable \code{g}, the new column is
named \code{x_mean_g}. For example, with grouping variables \code{"firm"} and \code{"year"},
variable \code{"labor"} yields columns \code{labor_mean_firm} and \code{labor_mean_year}.

If no grouping variable is supplied and \code{data} is not a \code{panel_data} object,
the overall mean of each numeric variable is added as a constant column named
\code{x_mean}.

The returned object has class \code{"panel_data"} if the input was a \code{panel_data} object;
otherwise it is a \code{data.frame} with additional attributes.
In both cases, the object has the following attributes:
\describe{
\item{\code{metadata}}{List containing the function name and the arguments used.
If the input was a \code{panel_data} object, the original metadata elements
(entity, time, and delta) are preserved.}
\item{\code{details}}{A list containing data.frames with the computed group means.
If overall means were computed (no grouping), it contains a named vector
of the overall means.}
}
}
\examples{
data(production)

# Basic usage with a single group
prod_means_1 <- add_means(production, group = "firm")

# Using multiple grouping variables
prod_means_2 <- add_means(production, group = c("firm", "year"))

# With panel_data object (automatically uses entity and time)
panel <- make_panel(production, index = c("firm", "year"))
panel_means <- add_means(panel)

# Overall means (no grouping)
prod_overall <- add_means(production)

# Accessing attributes
attr(panel_means, "metadata")
attr(panel_means, "details")

}
\seealso{
See also \code{\link[=make_demeaned]{make_demeaned()}}, \code{\link[=summarize_numeric]{summarize_numeric()}}, \code{\link[=plot_heterogeneity]{plot_heterogeneity()}}.
}
