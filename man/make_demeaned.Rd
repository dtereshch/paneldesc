% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/make_demeaned.R
\name{make_demeaned}
\alias{make_demeaned}
\title{Within-Group Demeaning (Centering) for Panel Data}
\usage{
make_demeaned(data, group = NULL)
}
\arguments{
\item{data}{A data frame (or \code{panel_data} object) containing the variables
to be demeaned.}

\item{group}{A character string or vector of character strings specifying
the grouping variable(s). If \code{NULL} and \code{data} is a \code{panel_data}
object with valid panel attributes, the entity and time variables
are used as grouping variables. Otherwise, overall demeaning is
performed.}
}
\value{
A data frame (or \code{panel_data} object if the input had panel
attributes and the default grouping was used) where all numeric
variables have been replaced by their demeaned versions.
Non‑numeric variables and the grouping variables themselves remain
unchanged. Rows with missing values in any grouping variable are
removed (with a message) before demeaning. The returned object
has a \code{metadata} attribute containing \code{function_name} and \code{group}.
}
\description{
This function performs within-group demeaning (centering) for all numeric
variables in a data frame. For each group defined by the \code{group} argument,
the group mean is subtracted from each observation. If no grouping is
provided, the overall mean is subtracted (grand mean centering).
}
\details{
The function mimics the within‑transformation used in fixed‑effects
estimators such as \code{fixest::feols()} or \code{linearmodels.AbsorbingLS}.
\itemize{
\item If \code{group = NULL} and \code{data} is \strong{not} a \code{panel_data} object, or if
\code{data} is a \code{panel_data} object but the required panel metadata are
missing, simple overall demeaning is performed: for each numeric
variable, the overall mean (ignoring \code{NA}s) is subtracted.
\item If \code{group} is specified (non‑\code{NULL}), the grouping variables are used
to define the groups. Observations with \code{NA} in any grouping variable
are removed before demeaning.
\item If \code{data} inherits from \code{panel_data} and \code{group = NULL}, the function
automatically uses the entity and time variables stored in the
\code{metadata} attribute as grouping variables, and the returned object
retains the \code{panel_data} class and its attributes.
}
}
\examples{
data(production)

# Simple overall demeaning
prod_demeaned <- make_demeaned(production)
head(prod_demeaned$labor)          # centered around overall mean

# Demeaning by a single group (e.g., firm)
prod_demeaned_firm <- make_demeaned(production, group = "firm")

# Demeaning by multiple groups (e.g., firm and year)
prod_demeaned_both <- make_demeaned(production, group = c("firm", "year"))

# Using a panel_data object: automatically demeans by firm and year
panel <- make_panel(production, index = c("firm", "year"))
panel_demeaned <- make_demeaned(panel)
class(panel_demeaned)               # still "panel_data"

}
\seealso{
\code{\link{make_panel}} for creating \code{panel_data} objects,
\code{\link{decompose_numeric}} for variance decomposition,
\code{\link{plot_heterogeneity}} for visualising group heterogeneity.
}
