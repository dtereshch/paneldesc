% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/make_balanced.R
\name{make_balanced}
\alias{make_balanced}
\title{Create a Balanced Panel Dataset}
\usage{
make_balanced(data, index = NULL, delta = NULL, balance = "rows")
}
\arguments{
\item{data}{A data.frame containing panel data in a long format, or a \code{panel_data}
object created by \code{\link[=make_panel]{make_panel()}}.}

\item{index}{A character vector of length 2 specifying the names of the entity and
time variables. For \code{panel_data} objects, this is optional and will be
extracted from the object's attributes.}

\item{delta}{An optional integer giving the expected interval between time periods.
For \code{panel_data} objects, this is optional and will be extracted from the
object's attributes if not provided.}

\item{balance}{One of \code{"rows"}, \code{"entities"}, or \code{"periods"}. Specifies the
balancing method (see Details). Default is \code{"rows"}.}
}
\value{
A balanced \code{panel_data} object (data.frame with additional attributes).
}
\description{
This function creates a balanced panel dataset by either keeping only entities
present in all time periods, keeping only periods where all entities are present,
or expanding the data to include all entity-time combinations.
}
\details{
This function balances a panel dataset according to the chosen method.
The returned object has class \code{"panel_data"} and includes metadata attributes
similar to \code{\link[=make_panel]{make_panel()}}.

\strong{Balancing methods:}
\describe{
\item{\code{balance = "rows"}}{Create a row for every entity‑time combination.
If \code{delta} is supplied, the full time sequence (including missing periods)
is used. Missing combinations get \code{NA} in all other columns.}
\item{\code{balance = "entities"}}{Keep only entities present in \strong{all} time periods.}
\item{\code{balance = "periods"}}{Keep only time periods where \strong{all} entities are present.}
}

\strong{Duplicates:} If duplicate entity-time combinations exist, the function stops
with an error, as balancing requires a unique key.

\strong{Missing values:} Rows with missing entity or time values are automatically removed
before balancing.

\strong{Handling of \code{panel_data} objects:} If \code{data} is a \code{panel_data} object, the
function will use the entity, time, and delta values stored in its attributes
unless overridden by explicit \code{index} or \code{delta} arguments.
}
\examples{
data(production)

# Create a panel object first
panel <- make_panel(production, index = c("firm", "year"))

# Expand to full grid (default method)
balanced_rows <- make_balanced(panel)

# Keep only entities present in all periods
balanced_entities <- make_balanced(panel, balance = "entities")

# Keep only periods where all entities are present
balanced_periods <- make_balanced(panel, balance = "periods")

# Using a regular data frame (index must be provided)
balanced_rows2 <- make_balanced(production,
  index = c("firm", "year")
)

# Specifying time interval for yearly data
balanced_rows_delta <- make_balanced(panel, delta = 1)

}
\seealso{
See also \code{\link[=make_panel]{make_panel()}}, \code{\link[=describe_dimensions]{describe_dimensions()}}, \code{\link[=describe_balance]{describe_balance()}}.
}
