% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/make_balanced.R
\name{make_balanced}
\alias{make_balanced}
\title{Create a Balanced Panel Dataset}
\usage{
make_balanced(data, index = NULL, delta = NULL, balance = "rows")
}
\arguments{
\item{data}{A data.frame containing panel data in a long format.}

\item{index}{A character vector of length 2 specifying the names of the entity and
time variables.
If not specified and data is a \code{panel_data} object, the entity and time values
will be extracted from the data.frame attributes.}

\item{delta}{An optional integer giving the expected interval between time periods.
If not specified and data is a \code{panel_data} object with defined \code{delta},
the value will be extracted from the data.frame attributes.}

\item{balance}{One of "rows", "entities", or "periods". Specifies the
balancing method (see Details). Default = "rows".}
}
\value{
A balanced panel data.frame with additional attributes.
}
\description{
This function creates a balanced panel dataset by either keeping only entities
present in all time periods, keeping only periods where all entities are present,
or expanding the data to include all entity-time combinations.
}
\details{
This function balances a panel dataset according to the chosen method.
The returned object has class \code{"panel_data"} and includes metadata attributes
similar to \code{\link[=make_panel]{make_panel()}}.

Depending on the value of \code{balance}, the balancing is performed using different methods:
\describe{
\item{\code{balance = "rows"}}{Create a row for every entity-time combination.
If \code{delta} is supplied, the full time sequence (including missing periods)
is used. Missing combinations get NA in all other columns.}
\item{\code{balance = "entities"}}{Keep only entities present in all time periods.}
\item{\code{balance = "periods"}}{Keep only time periods where all entities are present.}
}

The returned object has class \code{"panel_data"} and two additional attributes:
\describe{
\item{\code{metadata}}{List containing the function name and the arguments used.}
\item{\code{details}}{List containing the unique entities and periods in the balanced data, and if \code{delta} is supplied, the restored full period sequence and any missing periods.}
}
}
\note{
An entity-time combination is considered \strong{present} if the corresponding row contains at least
one non‑NA value in any substantive variable (all columns except the entity and time identifiers).
}
\examples{
data(production)

# Expand to full grid (default method)
balanced_rows <- make_balanced(production, index = c("firm", "year"), balance = "rows")

# Keep only entities present in all periods
balanced_entities <- make_balanced(production, index = c("firm", "year"), balance = "entities")

# Keep only periods where all entities are present
balanced_periods <- make_balanced(production, index = c("firm", "year"), balance = "periods")

# With panel_data object
panel <- make_panel(production, index = c("firm", "year"))
balanced_panel <- make_balanced(panel)

# Specifying time interval
balanced_rows_delta <- make_balanced(production, index = c("firm", "year"), delta = 1)

# Accessing attributes
attr(balanced_panel, "metadata")
attr(balanced_panel, "details")

}
\seealso{
See also \code{\link[=make_panel]{make_panel()}}, \code{\link[=make_wide]{make_wide()}}, \code{\link[=make_long]{make_long()}}, \code{\link[=describe_dimensions]{describe_dimensions()}},
\code{\link[=describe_balance]{describe_balance()}}.
}
