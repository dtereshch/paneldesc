% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_periods.R
\name{plot_periods}
\alias{plot_periods}
\title{Time Coverage Distribution Visualization}
\usage{
plot_periods(data, index = NULL, colors = c("#1E4A3B", "white"))
}
\arguments{
\item{data}{A data.frame containing panel data in a long format.}

\item{index}{A character vector of length 2 specifying the names of the entity and time variables.
Not required if data has panel attributes.}

\item{colors}{A character vector of length 2 specifying the fill color and line color for the histogram.
First color is for fill, second color is for the border line. Default = c("#1E4A3B", "white").}
}
\value{
Invisibly returns a list with summary statistics and metadata.
}
\description{
This function calculates summary statistics and creates a histogram showing
the distribution of time periods covered by each entity in panel data.
}
\details{
The function creates a histogram of the number of time periods covered by each entity.
The x‑axis shows coverage (periods per entity), the y‑axis shows the count of entities.

The returned list contains:
\describe{
\item{\code{metadata}}{List containing the function name and the arguments used.}
\item{\code{details}}{List with the coverage vector per entity and the histogram data used for plotting.}
}
}
\note{
An entity-time combination is considered \strong{present} if the corresponding row contains at least
one non‑NA value in any substantive variable (all columns except the entity and time identifiers).
}
\examples{
data(production)

# Basic usage
plot_periods(production, index = c("firm", "year"))

# With panel_data object
panel <- make_panel(production, index = c("firm", "year"))
plot_periods(panel)

# Custom colors
plot_periods(production, index = c("firm", "year"), colors = c("gray", "black"))

# Accessing list components
out_plo_per <- plot_periods(production, index = c("firm", "year"))
out_plo_per$metadata
out_plo_par$details

}
\seealso{
See also \code{\link[=describe_periods]{describe_periods()}}, \code{\link[=plot_patterns]{plot_patterns()}}, \code{\link[=plot_missing]{plot_missing()}}.
}
